{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "shopping-agent",
      "runtimeExecutable": ".venv/bin/python",
      "runtimeArgs": ["-m", "uvicorn", "shopping_agent.main:app", "--reload", "--host", "0.0.0.0", "--port", "8000", "--ssl-certfile", "localhost+2.pem", "--ssl-keyfile", "localhost+2-key.pem"],
      "port": 8000
    }
  ]
}
